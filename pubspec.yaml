name: potato
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.4.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  encrypt: ^5.0.0
  flutter_html: ^3.0.0-beta.2
  webview_flutter: ^4.8.0
  better_player: ^0.0.84
  video_player: ^2.9.1
  go_router: ^13.2.0
  provider: ^6.1.2
  cupertino_icons: ^1.0.6
  flutter_launcher_icons: ^0.13.1
  flutter_staggered_grid_view: ^0.7.0
  # fijkplayer: ^0.11.0
  shared_preferences: ^2.3.1
  fijkplayer:
    git:
      url: https://github.com/GalenBlabla/fijkplayer.git
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

flutter_icons:
  android: true
  ios: true
  image_path: "assert/maomaoicon.png"
